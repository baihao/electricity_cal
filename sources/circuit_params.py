"""
电路参数配置文件

集中管理所有仿真阶段使用的电路元器件参数
"""

# ==================== 基本电路参数 ====================
C = 36e-3  # 36 mF = 0.036 F
R = 500.0  # 500 Ω
Rg = 0.5  # 0.5 Ω
L = 30e-6  # 30 µH = 0.00003 H

# ==================== 非线性器件参数 ====================
# 二极管参数
U_D1_THRESHOLD = 0.75  # D1、D2、D3、D4导通阈值电压（V）
U_D2_THRESHOLD = 0.75  # D2导通阈值电压（V），与D1相同
U_D3_THRESHOLD = 0.75  # D3导通阈值电压（V），与D1相同
U_D4_THRESHOLD = 0.75  # D4导通阈值电压（V），与D1相同

R_D1 = 0.00007  # D1等效电阻（Ω）
R_D2 = 0.00007  # D2等效电阻（Ω）
R_D3 = 0.00007  # D3等效电阻（Ω），与D1相同
R_D4 = 0.00007  # D4等效电阻（Ω），与D1相同

# 可控硅参数
U_SCR_THRESHOLD = 0.88  # SCR1导通阈值电压（V）
R_SCR = 0.000052  # SCR1等效电阻（Ω）

# ==================== 整流桥参数（计算得到）====================
# 整流桥总阈值电压和总等效电阻
# 正向路径 (i_L > 0): D1 + SCR1 + D2
# 反向路径 (i_L < 0): D3 + SCR1 + D4
U_BYPASS_THRESHOLD = U_D1_THRESHOLD + U_SCR_THRESHOLD + U_D2_THRESHOLD  # 2.38 V
R_BYPASS_TOTAL = R_D1 + R_SCR + R_D2  # 0.000192 Ω

# ==================== Stage 3 特有参数 ====================
R_K3 = 0.001  # K3闭合时的等效电阻（Ω），假设很小
I_SCR_OFF_THRESHOLD = 0.1  # 晶闸管关闭阈值（A），当电流小于此值时，晶闸管关闭且不再开启

# ==================== 初始电压条件 ====================
U0_CASE1 = 0.0  # 初始电压情况1：0V
U0_CASE2 = 750.0  # 初始电压情况2：750V

